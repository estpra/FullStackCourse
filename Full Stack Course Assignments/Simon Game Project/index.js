//this line of code adds an event listener for a mouse click that return true or false whether the classList of the targeted element(target) contains the word in the parameters, will be useful for the switch statement we may need to add
$(".btn").click((event)=> {
    console.log(event.target.classList.contains("green"))

    //This logic will be useful for the highScore variable
$("span").text("Like DJ Kahlid")
console.log(Math.floor(Math.random() * 4))
})

